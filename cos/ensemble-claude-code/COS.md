---
type: cos-manifest
cos: ensemble-claude-code
family: ensemble
provider: claude-code
target-slot: isolated sibling profile home (host specifics live in the local deployment state)
lifecycle-state: deployed (smoke-verified 2026-07-08 — the P8 acceptance reading, all seven criteria met, lives in the pursuit's pass record iteration 09; real-use verification is owned by the charter candidates)
date: 2026-07-07
authored: 2026-07-08
deployed: 2026-07-08 (user-executed, user-attested)
provenance: pursuit claude-code-cos-realization (contracts/2026-07-07-claude-code-cos-realization/)
---

# Ensemble — Claude Code realization

The first COS grown in this workbench: the **Ensemble** shape — a closed team of specialized roles playing one piece under the human's direction — realized for the Claude Code harness. The family name names the conceptual shape; each realization of it for a harness is its own COS, this being the first.

Governing pursuit: `contracts/2026-07-07-claude-code-cos-realization/` (contract, spec, plan). Design of record: that spec plus the worldview canon in the Shared External Brain. The generalized commitments the shape honors are stated in the contract's aim facet; what each staged element carries is accounted in `DISPOSITIONS.md`.

Nothing lands here that the spec has not locked. The admission law (spec OD-5 amendment, 2026-07-08) governs entry: every artifact in this body carries a provenance header line naming its door — a curation record in `curation/claude-code/` for foreign and doctrine-foreign material, or a designated work unit's word-gated work for native material. The whole v1 set traces to the realization pursuit.

## Body layout and deploy map

- `always-on/CLAUDE.md` → the home's `CLAUDE.md` — the identity plate.
- `always-on/rules/` → the home's `rules/` — the four worldview modules plus the dispatch-discipline module.
- `agents/` → the home's `agents/` — the five staged members (the Concertmaster is the main session itself and has no file).
- `skills/` → the home's `skills/` — the five protocol runbooks, plus (deployed, footprint-verified 2026-07-09) the **curated obsidian trio** (`obsidian-markdown`, `obsidian-bases`, `json-canvas`), three tailored Agent Skills admitted in part through the curation door (record `curation/claude-code/records/obsidian.md`, tending third-party-curation, obsidian beat — the second curation pass, and the first admitted via the direct-placement *skill* vehicle rather than MCP wiring). Each ships a `references/` folder; the deploy script's skills-copy loop was extended to carry references (the native runbooks have none). Two of the bundle's five skills (obsidian-cli, defuddle) were rejected and are absent by design. The set grows a third class (**deployed, footprint-verified 2026-07-09** — the user's own `-Update`, a fresh session listing all 14 user skills including the six, references resolving on host, no startup warnings, plate steady 417): the **six promoted formal-library skills** — `cross-shell-command-discipline`, `skill-frontmatter-discipline`, `decision-proposal-discipline`, `felt-intent-extraction` (carrying `strict-ontological-articulation`'s ontological audit as a `references/ontological-audit.md` slice, no second entry), `ubiquitous-language-stewardship`, and `mermaid-multiview-learning-document` (with four `references/`) — model-neutral craft and dialogue method the shape was not authored to carry natively. These enter through the **designated-work door** (native first-party material, word-gated work — not the curation door): pursuit `formal-skills-promotion`, the triage map agreed 2026-07-09 on the user's word at `contracts/2026-07-09-formal-skills-promotion/routes/triage-map/map.md` (6 promoted, 10 held, 20 folded). The deploy set grows 26 → 37 (the six `SKILL.md` files plus five `references/` files). A further native skill enters through the same designated-work door (**deploy set 37 → 38**): **`operational-lane-discipline`**, the moment-loaded runbook of the operational-lane concept (admitted in part on the user's word 2026-07-11, meaning of record `Aim.md`'s operational-lanes entry and the Shared External Brain canon note ported in parallel; the always-on worldview deliberately carries no lane text yet, deferred on-trigger). One `SKILL.md`, no `references/`.
- `hooks/` → the home's `hooks/` — the SessionEnd litter-flag script, the two path/bash guard scripts (`guard-archivist-paths.sh`, now dual-wired to serve the main session as well as the Archivist, and `guard-examiner-bash.sh`), and the session-level push-gate guard (P8 probe evidence: prefix ask rules alone were evaded by the flag-first command shape).
- `settings.json` → the home's `settings.json` — default model, auto-memory disabled plus the memory-deny backstop, SessionEnd and SessionStart hook wiring (the litter-flag at session end, the `[compaction-marker]` echo at post-compaction start — the observable trigger the retrieval rule's compaction bullet keys on), and the curated tool-permission rules. The **claude.ai account-connector door**, first closed structurally by a single blanket `mcp__claude_ai_*` deny (2026-07-08), is now (admitted at the gate 2026-07-09, **deployed and footprint-verified 2026-07-09**) a **layered design** admitting the Gmail + Google Drive connectors through curation: an `mcp__claude_ai_*` ask floor (every connector tool, current and future, prompts) plus 21 exact-string ask entries (startup-typo-check tripwires) plus six narrow deny tripwires (Calendar/Supabase wholesale; Gmail `send*`/`delete*`; Drive `share*`/`delete*`), replacing the blanket deny — records `curation/claude-code/records/gmail.md` and `curation/claude-code/records/google-drive.md` (tending third-party-curation, beat 2026-07-09-3). The deny-removal was user-approved 2026-07-09 and the connectors were admitted at the gate 2026-07-09 (the user's word); the Operator-envelope and dispatch-module gate acts are ridden, and the deploy plus the post-deploy footprint check were the user's hand (`-Update`, 26 files hash-verified; the 21 tools enumerated live with exact spellings, the ask floor observed firing on a Gmail search, Calendar/Supabase absent on this host, a Gmail label write-pair and a Drive `create_file` passed under Operator confirm-first, the main-session doctrine probe declined self-serve). The playwright rules (the `mcp__playwright__*` ask and the two RCE denies) also live here.
- `.mcp.json` → the home's `.mcp.json` — **staged empty by decision** (`{"mcpServers": {}}`); it is not a harness-read Stage for user-scope MCP wiring (that lives in the home's `.claude.json`), so it carries no server. Kept in the deploy set deliberately so a re-deploy overwrites any stale stanza a prior mis-staging left in the live home (the deploy script has no delete step). The **playwright** OSCC realization (browser live-control) is admitted at the gate 2026-07-09 (the user's word) through the curation door (record `curation/claude-code/records/playwright.md`, tending third-party-curation, beat 2026-07-09-1) and wired by the user-run `launch/wire-mcp.ps1` into the home's user-scope `.claude.json`; deployed and footprint-verified 2026-07-09 (the user's live run — wire-run done, server connected pinned `0.0.77`). All wiring enters only through curation.
- `DISPOSITIONS.md` — the AC-7 coverage account; body documentation, never deploys.
- `launch/` → the home's `launch/` — the session launcher (`start-ensemble.ps1`): starts a session inside the home from any directory via the isolation lever (`CLAUDE_CONFIG_DIR` + `--setting-sources user`), leaving the calling shell's environment untouched. It also sets fullscreen TUI rendering as a launch default (`CLAUDE_CODE_NO_FLICKER` plus the `CLAUDE_CODE_ALT_SCREEN_FULL_REPAINT` Windows Terminal repaint fix), set-if-unset so a session override wins (TUI-fullscreen wish, `Aim.md` 2026-07-24; the `.sh` launcher mirrors it). Also `wire-mcp.ps1` — the one-time, user-run MCP wiring script that registers curated servers (first: playwright) into the home's user-scope `.claude.json`; hand-run like the launcher because `.claude.json` is harness runtime state the deploy script never touches (record `curation/claude-code/records/playwright.md`). Also `cos.ps1` — the **delegation-only `cos` dispatcher** (pursuit `shell-ergonomics`, 2026-07-24; deploy set 38 → 39, hash-verified): a PowerShell function offering `cos launch` / `cos update` / `cos probe` / `cos wire-mcp` / `cos help`, each a thin `& <script> @args` call to a canonical script — no invocation logic re-implemented, and `cos release` deliberately absent (its gate-adjacent bound). Deployed-side subcommands (`launch`, `wire-mcp`) call the copies under `%LOCALAPPDATA%`; repo-side subcommands (`update`, `probe`) locate `deploy-to-host.ps1` / `probe-context.ps1` via the **`OSCC_WORKBENCH`** env anchor — the workbench repo root, user-set in the gitignored host-local companion, never in tracked source (publishable-clean). The one-word profile entry is a dot-source of the deployed copy, `. "$env:LOCALAPPDATA\ensemble-claude-code\launch\cos.ps1"`, installed by the user's own hand under its own gate; it supersedes the retired `function ensemble { ... }` one-liner. The bash `cos` sibling is deferred on-trigger.
- `deploy-to-host.ps1` — the user-executed deploy hop; realizes this map on the host and prints the footprint. Never deploys itself.
- The **Linux/macOS bash trio** — `deploy-to-host.sh`, `launch/start-ensemble.sh`, `launch/wire-mcp.sh` — mirrors the PowerShell trio's behavior for the public edition's adopters on Linux and macOS, deploying into the XDG config home (`${XDG_CONFIG_HOME:-$HOME/.config}/ensemble-claude-code`, overridable via `CLAUDE_ENSEMBLE_HOME`; the config-home analog of `%LOCALAPPDATA%`). These are body files carried wholesale by the public projection; they are **not** in the Windows deploy script's byte-identical hash set and do not deploy on a Windows run (the `.sh` deploy copies `launch/*.sh` only, the `.ps1` deploy `launch/*.ps1` only). Deploy and launch mechanics smoke-tested on WSL Ubuntu (bash 5.2); `wire-mcp.sh` live-confirmed 2026-07-22 via the user's clone-and-deploy of the public edition on WSL Ubuntu (projection byte-identical to source, so the evidence transfers) — pinned Playwright server registered into the new home and connected in a live session after the one-time login. macOS still untested (the deploy script falls back from `sha256sum` to `shasum -a 256`).

Deployed 2026-07-08 by the **user-executed** final hop (`deploy-to-host.ps1`, run clean and user-attested) — the corrected shape after a session-side deploy was falsified from ground truth (the session-boundary lesson: footprints drawn only inside a session are not host evidence; full record in `templates/DEPLOYMENT.local.md`). The strip decision stands: the always-on provenance comment lines come off the deployed copies, the source keeps them. The session-side observations (settings with `_provenance` keys load; the SessionEnd litter-flag hook runs end-to-end) hold as behavior evidence of the staged set. Per the evidence discipline, deployed is not verified: P8 (footprint smoke + acceptance, host-side probes user-run or user-verified) enters after the one-time login in the new home. The session launcher (`launch/`) was added after the first host copy and reached the home with a `-Force` re-run at the time; staged-set refreshes since ride the script's `-Update` mode, which preserves login, trust, and session state.
