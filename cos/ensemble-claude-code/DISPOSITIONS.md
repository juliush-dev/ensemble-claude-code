---
type: cos-dispositions
cos: ensemble-claude-code
pursuit: claude-code-cos-realization
date: 2026-07-08
provenance: pursuit claude-code-cos-realization (contracts/2026-07-07-claude-code-cos-realization/)
---

# Dispositions — what carries what, and what was left out

The AC-7 coverage account for the Ensemble's Claude Code realization: for each of the contract's eight-plus-one commitments, the staged elements that carry it; then what was deliberately left unused, with reasons; then the authoring-time footprint checks and measurements this authoring rests on. Maintained with the body; the deploy-time and smoke-time evidence (P7/P8) will extend it.

## Coverage by commitment

1. **A team, not a configured tool.** Carried by: the identity plate (the closed six-role roster named always-on); the five member files in `agents/` each earning its context window by kind of work; the never-plugin-packaged placement (members live in the home's `agents/`, so no envelope field is silently dropped).
2. **The human directs, the shape classifies and executes.** Carried by: the dispatch-discipline module (auto-classification, announced dispatch, ask-first conditions, hard human gates, self-answer boundary); the Operator's confirm-first definition; the `designate` runbook's floor (no ceremony forced onto plain conversation).
3. **The shape owns cohesion, the human owns progression.** Carried by: consequence-complete editing binding the writing members (dispatch module, Builder file); the Examiner's consistency reading (surfaces, not diffs); the Concertmaster's consequence-closure duty on every return (identity plate, dispatch module); the drift-watch charter candidate registered in the workbench Registry.
4. **Authority enveloped, commitments manufactured.** Carried by: fixed per-member tool envelopes (allow-lists in every member file); the Examiner's structural edit refusal (`disallowedTools`) plus its Bash guard; the Archivist's path guard bounding auto-acceptance to notebook surfaces; the permission deny set; the envelope law in the dispatch module (instructions never widen an envelope). Member envelopes grow only through the curation gate — the user's word plus a governed edit — never through instructions or self-expansion; playwright (2026-07-09) is the first exercised precedent. The **obsidian** curation pass (third-party-curation, obsidian beat, 2026-07-09; record `curation/claude-code/records/obsidian.md`) is the second, and shows the gate governs more than tool envelopes: it admitted three instructional Agent Skills (obsidian-markdown, obsidian-bases, json-canvas) that grow the curated **skill set** without widening any member's tool envelope, and rejected two skills at the same gate for envelope contradiction (obsidian-cli's host-RCE-class eval/CDP has no lawful consuming envelope) and uncontrolled-executable float (defuddle). Admitted as tailored 2026-07-09 (the user's word); deployed and footprint-verified 2026-07-09. The **claude.ai Gmail + Google Drive connectors** (third-party-curation, beat 2026-07-09-3; records `curation/claude-code/records/gmail.md`, `curation/claude-code/records/google-drive.md`) are the **third precedent** — and the first where the gate *loosens a standing structural boundary rather than adding one*: it replaces the blanket `mcp__claude_ai_*` deny with a layered ask-floor + exact-string + narrow-deny design, admitting 21 live-propagated tools under all-ask with strengthened confirm-first. The envelope still grows only through the gate (the deny-removal was user-approved 2026-07-09 after a classifier escalation, never a silent edit); admitted at the gate 2026-07-09 (the user's word), the Operator-envelope and dispatch-module co-moves ridden the same day, **deployed and footprint-verified 2026-07-09** (the user's live run — 21 tools enumerated with exact spellings, ask floor observed firing, a Gmail label write-pair and a Drive `create_file` passed under Operator confirm-first, the doctrine probe declined self-serve under direct pressure). The admission law's **other door — the designated-work door** (native first-party material through a work unit's word-gated work, not the curation gate) — records its **first bulk use** here: pursuit `formal-skills-promotion` (triage map agreed 2026-07-09 on the user's word, `contracts/2026-07-09-formal-skills-promotion/`) promoted **six formal-library skills** into the curated skill set (`cross-shell-command-discipline`, `skill-frontmatter-discipline`, `decision-proposal-discipline`, `felt-intent-extraction` carrying the strict-ontological audit slice as `references/`, `ubiquitous-language-stewardship`, `mermaid-multiview-learning-document`; 10 held, 20 folded per the map). Like the obsidian trio these grow the skill set **without widening any member's tool envelope** — the envelope-only-through-curation rule is untouched — but the door differs: their content is first-party (attestable authorship, not foreign material), so it rides native word-gated work rather than a curation record. Deployed and footprint-verified 2026-07-09 (deploy set 26 → 37). The same door records its **second use** with the **operational-lane adoption** (admitted in part on the user's word 2026-07-11, meaning of record `Aim.md`'s operational-lanes entry): one native skill, `operational-lane-discipline` — the moment-loaded runbook of the operational-lane concept — again grows the skill set without touching any envelope. **Deploy set 37 → 38**; authored and staged 2026-07-11, **deployed 2026-07-11 by the user's `-Update`, observed in his same-day `/context`** (15 user skills listed, `operational-lane-discipline` live at ~120 listed tokens against the ~60–85 estimate — within the documented `/context` wobble; the ambient memory-files layer unchanged, wobble-consistent ~4.9k, as designed). The concept's always-on folds are deferred on-trigger, so nothing lands in the always-on layer here.
5. **Nothing silently lost in the split.** Carried by: this surface itself (coverage by disposition); the workspace-sized report contract with the grounding-status line on every return; the nomination duty (a member that cannot discharge names the gap and nominates); the admission law's provenance header on every body artifact (AC-5 as amended 2026-07-08) — every artifact here names its door, which for the whole v1 set is this pursuit's word-gated work; the two JSON files carry it as a `_provenance` key, whose clean load is a named P7 footprint item.
6. **Evidence-honest operations.** Carried by: the manifest's lifecycle honesty (authored ≠ deployed ≠ verified); the Examiner's evidence-before-claims definition; the `unit-close` runbook's discharge-rests-on-evidence rule; the amnesia module's settling duty; the SessionEnd litter-flag hook making silent litter visible.
7. **A shared long-term memory as the team's brain.** Carried by: the notebook institution in the worldview modules (capture, retrieval, freeze rule); the Archivist role; auto-memory refused structurally (`autoMemoryEnabled: false` as the organizational lever, deny rules as the refusal backstop, no member carries a `memory` field); the plate's closing sentence (team memory never agent-private).
8. **One conceptual shape, many providers, any work.** Carried by: the body/manifest cut (family Ensemble vs realization ensemble-claude-code); pattern-level wording throughout the always-on layer (no provider mechanics in the worldview modules); the workbench's many-bodies regions this body lives in.
9. **Every member individuates work identically from one staged ontology.** Carried by: the isolated sibling home as the organizational foundation — total subagent inheritance of the home's instruction hierarchy makes the worldview constitutive (no member exists without the native tongue); the four worldview modules authored fresh from the canon; the five runbooks carrying protocol depth; the session launcher (`launch/start-ensemble.ps1`) realizing the isolation lever (`CLAUDE_CONFIG_DIR` + `--setting-sources user`) as the everyday entry, so sessions reliably start inside the home rather than the default one; AC-2 (bare-host self-sufficiency) is this commitment's acceptance test at P8.

## Deliberately unused, with reasons

- **Auto-memory** — agent-private memory splits the team's brain; commitment 7 places memory in the shared notebook institution. Disabled by setting, denied by rules, smoke-probed at P8.
- **Per-turn injection (UserPromptSubmit context)** — nothing is re-injected per turn at v1; per-turn injection pays the digest again every turn (the budget-cap scar). The SessionStart re-injection remedy stages only on compaction-probe evidence (P8).
- **The per-turn Stop consequence-closure hook** — refused; Stop fires every turn's end, a per-turn cost. Consequence closure stays the Concertmaster's delivery-staged duty.
- **Output styles** — behavioral, not stably documented.
- **Agent teams** — experimental, env-gated.
- **Workflows** — no v1 staging need.
- **`.mcp.json`** — staged empty at v1 (an explicit `{"mcpServers": {}}`), so the decision was visible on disk rather than inferred from absence; tool wiring enters only through the curation pipeline. **The first curation pass has run** (playwright, tending third-party-curation, beat 2026-07-09-1), and its first footprint check corrected a Stage assumption: the config-home `.mcp.json` is **not** a harness-read Stage for user-scope MCP wiring — project scope reads `.mcp.json` from the working directory only; user scope lives in the home's `.claude.json` under top-level `mcpServers` (docs-settled 2026-07-09). So `.mcp.json` stays empty, and the admitted `@playwright/mcp` server (pinned `0.0.77`, `-y`, `--isolated`, explicit stdio) is registered at user scope by the user-run `launch/wire-mcp.ps1`, gated by the `mcp__playwright__*` ask rule and the `mcp__playwright__browser_run_code*` + `mcp__playwright__browser_run_code_unsafe` deny rules in `settings.json` (two-entry after the first-deploy deny missed the observed name — see the record). Admitted at the gate 2026-07-09; deployed and footprint-verified 2026-07-09 (wire-run done, server connected pinned `0.0.77`); record `curation/claude-code/records/playwright.md`. P8 probe 5 found a **second wiring door the file cannot close: claude.ai account connectors** ride the login into the home (Gmail and Google Drive arrived connected, uncurated). The decision was first carried structurally by a single blanket deny `mcp__claude_ai_*` in `settings.json` — every account-connector tool, current and future, in every permission mode (the user's word, 2026-07-08). Wanted connectors enter through curation like anything else, the admitting pass narrowing the rule deliberately — and **the third curation beat has now done exactly that** (Gmail + Google Drive, tending third-party-curation, beat 2026-07-09-3; records `curation/claude-code/records/gmail.md`, `curation/claude-code/records/google-drive.md`). The blanket deny is **replaced by a layered design** (admitted at the gate 2026-07-09, **deployed and footprint-verified 2026-07-09**; the deny-removal user-approved 2026-07-09 after a classifier escalation): an `mcp__claude_ai_*` **ask floor** (current-and-future connector tools all prompt — the "current and future" promise re-kept as *ask* rather than *deny*), **21 exact-string ask entries** (the enumerated Gmail/Drive tools, as startup-typo-check tripwires), and **six narrow deny tripwires** (Calendar/Supabase wholesale; Gmail `send*`/`delete*`; Drive `share*`/`delete*` — destructive/exfil classes absent from the enumeration, pre-empted). The priced weakening, taken on the user's word: a *future* connector tool surfaces ask-gated, not silently denied, until the next beat re-enumerates and re-tailors. Operator-envelope and dispatch-module gate-act co-moves ridden 2026-07-09 (the user's word); the deploy plus the post-deploy footprint check were the user's hand (the 21 tools enumerated live with exact spellings, the ask floor observed firing verbatim on a Gmail search, Calendar/Supabase absent on this host, the write pairs passing under Operator confirm-first, and — notable — the Drive connector confirmed to expose **no delete tool by design**, so a file removal escalated to the browser fallback, which stopped at Google's sign-in because the `--isolated` automation profile is unauthenticated, and the human deleted by hand). Calendar stays connected, held by its wholesale deny plus the ask floor until its own curation beat.
- **Plugin packaging for members** — plugin agents silently drop `hooks`/`mcpServers`/`permissionMode`; members live in the home's `agents/` only. The marketplace vehicle remains available for curated third-party material where plugin form fits — but **the first curation pass (playwright, tending third-party-curation, beat 2026-07-09-1) chose the direct-realization vehicle instead**, wiring `@playwright/mcp` at user scope via the user-run `launch/wire-mcp.ps1` (into the home's `.claude.json`) plus the `settings.json` rules and the `operator.md` envelope, rather than through a plugin. Plugin form did not fit: zero upstream bytes survive tailoring (the wrapper carries no code; the pin, `--isolated`, and stdio-only rewrite the launch stanza entirely), so a plugin would add an update channel and `enabledPlugins` state for no carried content and re-introduce the `@latest` float the pin exists to close. Vehicle choice is per-pass; plugin form stays on the table for material it actually fits. **The second curation pass (obsidian, third-party-curation, obsidian beat, 2026-07-09; record `curation/claude-code/records/obsidian.md`) also declined the marketplace vehicle, for a different reason:** the bundle's `source: "./"` installs the *whole repo* (all five skills), so the vehicle cannot carry a 3-of-5 admission without dragging the two rejected skills in uncurated. The three admitted skills are placed **directly** into the home's `skills/` (tailored in place, provenance-headed), exactly as the five native runbooks live there — the first admission via the direct-placement *skill* vehicle. Admitted at the gate 2026-07-09 (the user's word), deployed and footprint-verified 2026-07-09 (record `curation/claude-code/records/obsidian.md`).
- **First-party imports from the formal-skills library** — not taken at v1; the runbooks are authored fresh for whole-shape consistency, and imports are not exempt from curation.

## Boundaries versus mitigations (AC-3 honesty)

Binding boundaries ride structural and refusal contracts: tool allow-lists, `disallowedTools`, permission deny rules, `permissionMode`. The **hard human gates** (deploys, pushes, live external writes, broad restructures) are carried honestly as follows at v1: the dispatch discipline names them (a delivery contract), the Operator's live acts now have their **first deployed wired route** — the first curated family, playwright, rode the gate and went live (deployed, wire-run into the home's user-scope `.claude.json`, footprint-verified 2026-07-09): what is structural is the `settings.json` deny/ask layer (the `mcp__playwright__*` ask plus the `browser_run_code*`/`browser_run_code_unsafe` denies) together with the `mcp__playwright__*` glob in the Operator's envelope in `agents/operator.md`; what is *doctrinal* — a mitigation, not a boundary — is that only the Operator, under confirm-first and the untrusted-input doctrine, wields it, the per-act ask making each browser reach visible (record `curation/claude-code/records/playwright.md`), and push-shaped commands carry a **session-level PreToolUse push-gate guard** (`guard-push-gate.sh` on Bash|PowerShell: matches push-shaped git commands anywhere in the command string and downgrades them to an explicit ask) with the **staged ask rules as defense in depth** (`Bash(git push)`, `Bash(git push *)`, PowerShell twins). The guard is the binding carrier since P8 probe 10 leg C evidenced the prefix rules evaded by the ordinary flag-first shape (`git -C <path> push` prompted nothing — the user's word, 2026-07-08, remedied same day). **Both layers observed live after the `-Update`:** the ask rule prompting on the plain shape, the guard prompting on the previously-evading flag-first shape with its staged message verbatim. **The P8 closing acceptance reading (2026-07-08, in the pursuit's pass record, iteration 09): all seven criteria MET;** lifecycle `deployed (smoke-verified 2026-07-08)`; real-use verification stays owned by the charter candidates, not claimed. The session's permission mode is **pinned to auto deliberately** (`defaultMode: "auto"`, the user's word 2026-07-08 — he had been cycling to it manually every session): ordinary edits and commands are classifier-mediated, and everything the design calls binding stays on the mode-independent deny/ask rules. Deploy invocations stay classifier-mediated until the deploy-discipline charter fixes a command shape worth naming in an ask rule. Beyond that, two named gaps on this sandbox-less Windows host are **mitigations, not boundaries**, and are recorded as such:

- The Examiner's and Builder's **Bash write-escape**: no OS sandbox exists here. The Examiner's Bash guard narrows it (blocks write-shaped commands); the Builder's Bash legitimately writes. Outside-the-harness needs stay named, never papered over with instruction text.
- The **path guard** (`guard-archivist-paths.sh`) is **dual-wired** (pursuit guard-mediation-refinement, 2026-07-19). It **anchors on the session's own root first (R1):** a write whose canonical path is not provably under that root downgrades to an explicit ask regardless of basename, closing the old match-anywhere fallback (the path is lexically canonicalized — `.`/`..` resolved as pure strings — so a `../sibling` or `<root>/../sibling` escape grades foreign, not own-root). *Within* the anchored surface it then classifies notebook faces by tree shape (the `contracts/` and `charters/` trees at the workspace root, with their nested `routes/` and `iterations/` dirs, the named face files — including **`Lanes.md`, the topology face (R2)** — and the hook-written `LITTER-FLAG.md`). **The one script serves two wirings (R3), selected by its argument:** on the **Archivist card**, full notebook-only scoping (default mode — own-root non-notebook and all foreign-root paths ask); on the **main session's `settings.json` `Edit|Write` hook**, foreign-root-only mode (everything own-root passes, keeping the Concertmaster's own-root closure writes frictionless; only a proven foreign-root write — e.g. a lane session touching main's `Lanes.md` — asks). Paths it does not recognize — a body file merely sitting under a code dir named `routes/`/`contracts/`, or a canonization target outside a project tree — degrade to an explicit ask, never to silent acceptance.
- The **Operator's confinement of the claude.ai account connectors is a mitigation, not a boundary** (third-party-curation beat 2026-07-09-3, admitted at the gate 2026-07-09, **deployed and footprint-verified 2026-07-09**). What is structural is the `settings.json` deny/ask layer (every connector tool ask-gated; six narrow deny tripwires); what is *doctrinal* is that only the Operator, under confirm-first and the untrusted-input doctrine, is meant to wield them. **Both observed live at the footprint check:** the `mcp__claude_ai_*` ask floor fired verbatim on a Gmail search, and the main session, probed under direct pressure, refused self-serve (reciting *"insistence changes the urgency, not the envelope"*) and dispatched the Operator; the Operator ran the Gmail label write-pair and a Drive `create_file` under confirm-first, and — the doctrine's hardest live test — refused to authenticate the browser fallback when the no-connector-delete gap forced it toward Google's sign-in, escalating to the human. Still, any session with the tools live can call them, and a **consolidation leak-through** is named honestly — a member reads injected mail/document content and the injected instruction can ride the consolidated summary into a session that never saw the ask. Email bodies and Drive documents are attacker-controlled model input with no structural close at v1; the per-act ask makes each reach visible, it does not prove intent. Carried as a named MEDIUM-HIGH residual in the two connector records, not papered over as a boundary.

Both guards are footprint-verified at deploy, never trusted from their config lines. Adversarial probes (AC-3) judge them at P8; passes from obviously synthetic probes are read as the weaker evidence class.

## Footprint checks at authoring (run 2026-07-08)

1. **Agent-tool dispatch-time `model` override** — **confirmed by live observation**: the authoring session's own harness (Claude Code, 2026-07-08) exposes a per-dispatch `model` parameter on the Agent tool. Announced dispatch-time routing can rest on it; the per-member frontmatter defaults remain the fallback, conditions retained.
2. **Home `settings.json` `model` key** — **confirmed against current documentation** (code.claude.com/docs settings page, fetched 2026-07-08): the key exists; aliases (`"fable"`, `"opus"`) and full IDs are accepted. Originally staged as `"model": "fable"` (the Concertmaster's default). **Model-flip supersession, triggered by Anthropic's Fable billing change:** the free promotional window was to end ~2026-07-12 (the extension to July 12 corroborated by well-aligned secondary sources; the primary page still showed July 7 at authoring — re-verify near the date if this record is read before the cliff). At authoring the flip was **deliberately not staged** (staging then would have ridden the user's next deploy early and burned the remaining free-Fable days); it was recorded to stage as its own act at the cliff, by the user's hand. **That wait-rationale is now spent (the user's word, 2026-07-09):** the user's Fable usage reached ~89% of its limit, ahead of the ~2026-07-12 cliff, so the remaining free days were no longer worth holding for. **Staged 2026-07-09, source-side, then deployed and footprint-observed the same day:** `"model"` is `"opus"` in `settings.json`; the members stay on their existing opus/sonnet defaults (unchanged — "accordingly"). **Deployed 2026-07-09** by the user's `-Update` — source and host now agree on this key, no divergence. **Footprint-observed 2026-07-09** (the user's word): a fresh Ensemble session shows Opus live as the main agent. Fable escalation under the dispatch rule's named conditions (designation dialogue, architectural forks, Examiner acceptance gates) is now a **credit-priced act** — the routing doctrine itself is unchanged, this is only the economics note, now live rather than scheduled. **Flip back to fable, triggered by Anthropic's usage-limit reset:** Anthropic reset the 5h and weekly usage limits for everyone (the user's word, 2026-07-10), lifting the ~89%-of-limit pressure the 2026-07-09 flip was made under. **Staged 2026-07-10, source-side, then deployed the same day:** `"model"` is `"fable"` again in `settings.json`. **Deployed 2026-07-10** by the user's `-Update` (his word in-session, session-boundary rule satisfied — user-executed) — source and host agree again on this key, no divergence. **Watch note (unverified, from tweets, the user's word 2026-07-10):** the user reports a scheduled pull-off of the fable model around Sunday 7am. If fable becomes unavailable, expect a flip back to opus again; watch for this at the next deploys and re-verify the tweet-sourced claim against an authoritative surface before it becomes load-bearing.
3. **Auto-memory switch and deny syntax** — **confirmed against current documentation** (memory, sub-agents, and permissions pages, fetched 2026-07-08): `autoMemoryEnabled: false` disables the feature (staged — the organizational lever per the OD-4 condition); auto-memory lives at `<config-home>/projects/<project>/memory/`; `Edit(...)` deny rules cover Edit, Write, and NotebookEdit; a leading `/` anchors to the settings file's own directory, which for the deployed `settings.json` is the config home itself — so `Edit(/projects/**/memory/**)` and `Edit(/agent-memory/**)` deny the home's memory trees publishable-clean, with project-relative rules covering the project-scoped agent-memory dirs. Per-agent `memory` frontmatter exists (user/project/local scopes); no member sets it.
4. **Sizing of the always-on memory layer — measurement, anchor, and disposition.** Measured 2026-07-08 after the consistency-pass repairs and the admission-law provenance lines, with two estimators (chars÷4, conservative for markdown prose; words×1.33): identity plate **212–309**, four worldview modules combined **1,907–2,410**, whole layer **2,646–3,438**. **(Estimators retired 2026-07-08:** the live `/context` count — P8 probe 6 — measured plate 368 / modules ≈2,932 / **whole layer ≈4,195**; sizing is henceforth measured in the live home, never estimated.)** The layer then grew by individually gated adoptions to a measured **≈4,700–4,900** (the two 2026-07-10 `-Update` reads ~4.7k / ~4.6k; the evaluative-verdict-discipline folds read ≈4.9k, component sum ≈4,827, pre-accepted at that adoption's own gate as its priced cost, the user's word 2026-07-10) — **adoption arithmetic, ~+360–630 tokens of individually gated content since the 4,195 baseline, not drift**, with live `/context` reading wobble proven (`30-retrieval.md` 558 vs 659 for byte-identical content, ±10%). Through 2026-07-16 this layer was governed by a **self-defined token ceiling** (~4,000 → ~4,500 → ~4,700, each step amended upward under the accept-measured-reality precedent), growth past which returned to the gate.

    **(Ceiling-doctrine supersession, 2026-07-18 — the user's gate word "Admit, and also build the probe now so calibration can run at my next -Update"; the Examiner's ADMIT-IN-PART verdict is the agreed spec.)** Six corrections govern from here:

    - **No provider hard cap on this layer.** The home's `CLAUDE.md` and rules files load in full regardless of length (shorter files only aid adherence); the ~4,000 / ~4,500 / ~4,700 trail was a **house convention misread as a ceiling**, never a provider limit. Grounding of record: `grounding/2026-07-18-provider-max-ceilings.md`.
    - **The anchor is now a share, pinned to a tier — not a token count.** House sizing is tracked as the **measured share of the context window at session start, pinned to a named model/window tier** (instrument of record: the probe, per the calibration below; interactive `/context` gives the category grain). Both denominators stand: pre-calibration, the ~4,700–4,900-token layer read **≈0.47–0.49% of the main session's 1M window** (current Sonnet 5 / Opus 4.8 / Fable 5 tier — the deployed `model` is `fable`, 1M) and **≈2.35–2.45% of a 200K window** (Haiku members; gateway or `CLAUDE_CODE_DISABLE_1M_CONTEXT` configs); the calibrated readings of record are in the next bullet. A model/window flip **re-reads the anchor**; the raw token reading is kept alongside as the **tier-independent** quantity.
    - **The calibrated baselines are now of record (2026-07-18).** The **whole-startup share is 34,270 tokens ≈ 3.43%** of the 1M tier (the probe's API-counted startup total), and the **memory layer is ≈5k ≈ 0.50%** of the 1M window (per `/context` attribution, the accepted 2026-07-18 dialogue-adoption growth included). The prior fixed-number readings — the ~4,700–4,900-token layer at ~0.47–0.49% of the 1M window — stay as the **pre-calibration baseline history**, superseded by these calibrated figures, **never again amended upward as a "ceiling."** The plate (~430) and modules (~3,000) component figures carry the same disposition — baselines, not ceilings.
    - **The duty the old ceiling served is preserved.** Growth of the layer still **returns to the user's gate**; the tripwire now reads against the calibrated baselines — **measured growth beyond the recorded whole-startup share (the probe)** triggers attribution (interactive `/context`) and returns to the gate, not a fixed token count. The principle it serves, named: **frugality — maximum results with minimum necessary resources (the user's word, 2026-07-18).**
    - **The real provider caps that DO exist** (one reference block; citations in the grounding record): auto-memory `MEMORY.md` 200 lines / 25KB; skill listing 1% of the window and 1,536 chars/entry; post-`/compact` skill re-injection 5K per skill / 25K total; `@`-import depth 4; subagent depth 5 and 200 spawns/session (default, raisable via `CLAUDE_CODE_MAX_SUBAGENTS_PER_SESSION`). The always-on memory layer is **not** among these — see `grounding/2026-07-18-provider-max-ceilings.md`.
    - **The instrument. Calibration RUN and PASSED 2026-07-18** (user-executed): an interactive `/context` beside a same-host, same-day headless probe run, compared once — **offset ≈ −1.0k / ~2.9%** (probe below `/context`), inside the documented ±10% `/context` wobble; the probe reports **API-counted** usage where `/context` estimates. The headless probe (`probe-context.ps1`, sibling of `deploy-to-host.ps1`) is now the **instrument of record for the whole-startup share**; interactive `/context` is the **attribution instrument** — read when the tripwire fires or at deploy footprints, since the probe's usage fields carry no category breakdown (it measures the whole-startup share only). Before calibration, through 2026-07-18, `/context` stayed the measurement of record and the probe was to become the instrument only after the one-time calibration passed — that gate is now met. Per `grounding/2026-07-18-headless-context-metrics.md`.

**Deploy-time footprint results (P7, 2026-07-08 — corrected the same day):** a session-side deploy first drew a complete footprint (sha/strip integrity, settings load, the SessionEnd litter-flag hook end-to-end in a seeded probe) that the user then falsified from his own shell: the target did not exist on the real host — the session's writes outside its shared workspace were virtualized. Consequences, held apart honestly: the session-side observations stand as **behavior evidence of the staged set** (the settings file with its `_provenance` keys loads in a real harness; the hook resolves `$CLAUDE_CONFIG_DIR` under Git Bash, unescapes the stdin cwd, detects open records, and places the flag correctly) but count as **no host evidence at all**. The host copy is user-executed via `deploy-to-host.ps1`, whose printed output (baseline, strip, 15-file hash verification — 16 since the launcher landed, pass 06 — inventory) is the only deploy-time host footprint; the push ask rules and memory deny rules are staged but not behavior-probed (P8); the delivery footprint is additionally auth-blocked pending the one-time login and rides P8 with the real-tokenizer count and the adversarial probes — P8's host-side probes are likewise user-run or user-verified, per the session-boundary rule now ripening toward the deploy-discipline charter.

**P8 smoke results (accumulating; detail in the route's pass records):** Probe 1 (2026-07-08, user-run) **confirmed the settings-source exclusion**: inside an Ensemble session in a trusted folder carrying 63 project-level pre-approved permissions and extra workspace directories, `/permissions` shows none of them — Allow empty, workspace = original directory only — while the home's own four ask rules and four deny rules are live exactly as staged (four deny rules then; five since the `mcp__claude_ai_*` deny landed after probe 5). The isolation lever (`CLAUDE_CONFIG_DIR` + `--setting-sources user`) carries the whole-shape consistency boundary as designed. Probes 2–4 (2026-07-08, user-run): **worldview delivery confirmed** (roster, fork, menu scan spoken back exactly); **auto-memory refusal confirmed as behavior** (doctrine-aware refusal, capture routed to the shared external brain with co-moves, disk check clean under `projects/**/memory`); **grounding + report contract confirmed** (authoritative-source settling, grounding-status line). Three findings surfaced and were **resolved the same day on the user's word** (OD-4 amendment): the auto mode was the user's own deliberate per-session cycling, now pinned as `defaultMode: "auto"`; the dispatch module gained the capture-line scope sharpening (stewardship beyond the line dispatches to the Archivist) and the announced small-grounding self-serve threshold (real research to the Scout). Layer re-measured after the sharpenings: whole layer 2,725–3,545 (cap ~4,000 ✓); dispatch module 605–826. Probes 5–6 (2026-07-08, user-run): **the auto-fetched plugin-marketplace catalog is inert** — `/plugin` Installed shows only the home's own five runbook skills, no `enabledPlugins` key exists anywhere in the home, and the catalog sits unbought in the Marketplaces tab (the enabled-plugins check is a standing drift-watch item); and **the first real-tokenizer count landed** via `/context` in the live home — plate 368, worldview modules ≈2,932, dispatch 895, **whole layer ≈4,195** — superseding the estimator ranges above (they undercounted 20–45%; sizing is henceforth measured with `/context`, never estimated). Two findings resolved on the user's word (OD-3 amendment, 2026-07-08): the ceilings amended to measured reality (plate ~400 [later moved to ~430, 2026-07-09 — see item 4] / modules ~3,000 / whole layer ~4,500 [later moved to ~4,700, 2026-07-10 — see item 4], growth past a ceiling returns to the gate), and the **claude.ai account-connector door closed structurally** (probe 5 found Gmail/Drive riding the login in, uncurated; deny rule `mcp__claude_ai_*` staged, wanted connectors routed to the curation backlog). The same `/context` shot doubles as delivery evidence: all six always-on files, all five members, all five runbooks loaded. The deny rule's behavior check landed the same day after the user's `-Update`: `/permissions` shows `mcp__claude_ai_*` live and `/context` lists no MCP tools at all (previously 23 connector tools). **Probe 7 (2026-07-08, user-run): compaction and resume survival both confirmed** — the same worldview question answered exactly in a fresh session, after `--resume`, and after a real `/compact` (the harness observably re-read all five rules modules from disk right after compacting, the documented re-injection seen live); `/context` post-compaction lists all six memory files. The SessionStart re-injection remedy stays unstaged: its evidence condition never occurred. Honest measurement note: with byte-identical deployed files, `/context` per-file figures moved between readings (dispatch 895 → ~1,000; category 4.2k → 4.4k) — read the figures with ~±10% variance; the amended ceilings absorb it. **Probe 8 (2026-07-08, user-run, seeded-drift fixture): all folded verdicts PASS** — consequence-complete editing (three sites moved together, staleness judged with reasons), the Examiner's whole-surface reading (caught the planted drift as the HIGH finding plus two real inconsistencies nobody planted), the write refusal worldview-native at zero tool calls (the structural leg rests on the member definition — a tool never offered cannot be live-denied — and is read at the definition footprint under AC-3), announced dispatch and worldview-native member returns throughout. **Post-deploy footprint re-measure (2026-07-10, user-run `/context` after the `-Update` that deployed the retrieval-rule vocabulary enumeration — Aim item 38):** plate **417** (within the (then-)~430 plate ceiling); the **memory-files category reads ≈4.7k** — the five rules modules `10-work-object-model` 1.2k, `20-capture` 729, `30-retrieval` 659, `40-amnesia` 445, `50-dispatch` 1.3k, plus the plate; **14 user skills listed, five members, the deny/MCP posture as expected**. The plate held under its then-ceiling (a baseline, not a ceiling, since item 4's 2026-07-18 supersession), but the **whole always-on layer read ≈4.7k, past the (then-)OD-3 ~4,500 ceiling**, so growth-past-a-ceiling returned to the gate: the amend-to-measured-reality-vs-prune question was **resolved by the user's word 2026-07-10 — amend, ~4,500 → ~4,700** (item 4), under the accept-measured-reality precedent, the growth read as adoption arithmetic (~+360 tokens of individually gated content since the 4,195 baseline) rather than drift. The reading stays recorded as measured; the ~4,700 ceiling framing is **superseded 2026-07-18** (the fixed token ceiling retired for the `/context`-share anchor — see item 4), the ~4,700 reading kept as the baseline share, growth past which still returns to the gate. **Second-deploy footprint + recognition probe (2026-07-10, user-run):** a second user-run `-Update` the same day carried the P11-tailored cross-shell skill — **37 files hash-verified**, `skills\cross-shell-command-discipline\SKILL.md` in the inventory; a fresh `/context` read plate **417**, whole layer **≈4.6k**, `30-retrieval.md` **558** (byte-identical to the 659 reading above — the live wobble that item 4's ~4,700 amendment absorbs). **Recognition probe PASSED (user-run, fresh deployed session):** "what does OSCC mean?" → the agent named OSCC a protected operating-shape term, announced settling against surfaces rather than memory, read the Shared External Brain canon note, and returned the three-rung ladder correctly (OSCC / realization / Stage / Committing Contract). This is the vocabulary promotion's claimed contribution **observed in life through the footprint machinery** — condition 1 of its own admission-benefit run confirmed post-deploy, as the test prescribes.

Also grounded at authoring (same documentation pass): the SessionEnd hook config shape and its stdin JSON (`cwd` field — the litter-flag script reads it); hooks on Windows default to Git Bash (the scripts are bash); `CLAUDE_PROJECT_DIR` is documented for hook commands but **no config-home variable is documented** — the staged hook commands reference `$CLAUDE_CONFIG_DIR`, which exists in this home's sessions by construction (the isolation lever that activates the home sets it); this is a designed dependency, footprint-verified at P7. Per-agent `hooks` frontmatter confirmed (the two guards ride it); `effort` frontmatter confirmed (`low`–`max`; the Examiner stages `high`).
