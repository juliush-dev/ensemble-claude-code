{
  "_provenance": "pursuit framework-dna, route dna-into-the-cos (the user's word 2026-08-25: the Archivist writes in any repo without asking, doing no other member's job). Tracked EXAMPLE of the gitignored host companion settings.local.json: copy this file to settings.local.json beside it, fill in the real paths, never track the copy (publishable-clean: no usernames or machine paths in tracked files). deploy-to-host.ps1 / deploy-to-host.sh deep-merge the companion into the deployed settings.json (objects key by key, arrays concatenated without duplicates, companion scalars win); the tracked settings.json stays clean and the launcher's --setting-sources user makes the deployed file the only settings a session reads. Unchecked at staging: whether subagents inherit additionalDirectories - the user's live probe after deploy decides. The double-backslash path form is load-bearing: a single-backslash or forward-slash placeholder normalises to a drive-rooted Users-home form and fails the publication leak scan closed (publication/publisher/scan.py, _DRIVE_HOME). The third additionalDirectories entry (C:\\Users\\<your-username>\\AppData\\Local\\Temp\\claude) is the harness's per-session scratchpad root, added on the user's word 2026-08-25 so members write there without asking. Where the harness advertises that scratchpad in 8.3 short form (a <your-username>~1 segment, e.g. C:\\Users\\<SHORT~1>\\AppData\\Local\\Temp\\claude), the companion needs that short-form root as an additional entry too, since the harness's own additionalDirectories match is a separate layer from the guard and it is unchecked whether that layer canonicalises the path. On a Linux host the scratchpad root carries a per-uid segment, so the third entry is /tmp/claude-<uid> (observed 2026-08-25 as /tmp/claude-1000 on the user's Linux host; that the segment is id -u is inferred, unverified against docs); on macOS it is the TMPDIR-based form ${TMPDIR}/claude-<uid>, unlived.",
  "permissions": {
    "additionalDirectories": [
      "C:\\Users\\<your-username>\\Projects",
      "C:\\Users\\<your-username>\\Shared-External-Brain",
      "C:\\Users\\<your-username>\\AppData\\Local\\Temp\\claude"
    ]
  }
}
